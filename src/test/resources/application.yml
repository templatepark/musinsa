spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:musinsa-test;MODE=MySQL
    username: sa
    password:
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: never